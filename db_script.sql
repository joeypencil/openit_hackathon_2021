/* Table 'credible_refs' */
CREATE TABLE credible_refs(link TEXT NOT NULL, PRIMARY KEY(link));

/* Table 'infamous_refs' */
CREATE TABLE infamous_refs(link TEXT NOT NULL, PRIMARY KEY(link));

/* Table 'neutral_sites' */
CREATE TABLE neutral_sites(link TEXT NOT NULL, PRIMARY KEY(link));
